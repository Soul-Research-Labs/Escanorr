apiVersion: v1
kind: Service
metadata:
  name: { { include "escanorr.fullname" . } }
  labels: { { - include "escanorr.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "escanorr.selectorLabels" . | nindent 4 } }
